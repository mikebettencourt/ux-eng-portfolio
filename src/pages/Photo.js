import HomeButton from '../components/HomeButton';

function Photo() {
    return (
      <section className="photo-container">
        Photo
        <HomeButton />
      </section>
    );
  }
  
  export default Photo;